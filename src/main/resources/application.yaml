server:
  port: 8080

spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/ipr
    username: postgres
    password: password
  liquibase:
    change-log: classpath:db/changelog.xml